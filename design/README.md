# Maquettes

Direction artistique « Cuisine chaleureuse » (voir `vault/Inspiration UI/` et la
décision `D-2026-08-23 Direction Artistique Cuisine Chaleureuse`).

- `maquettes/*.dc.html` + `canvas.json` — artboards d'un canevas Claude Design ;
  chaque `.dc.html` s'ouvre aussi seul dans un navigateur.
- `maquettes/maquettes-maison.html`, `toasts-directions.html` — planches HTML
  autonomes des directions explorées.

Ce sont des explorations figées, pas la source de l'UI : le code de `web/` prime.
