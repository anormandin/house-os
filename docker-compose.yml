services:
  postgres:
    image: postgres:17-alpine
    container_name: houseos-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: houseos
      POSTGRES_USER: houseos
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-houseos-dev}
    ports:
      - "5433:5432"   # 5433 côté hôte : 5432 est déjà pris par un autre projet sur le Mac de dev
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U houseos -d houseos"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: houseos-app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ConnectionStrings__HouseOs: "Host=postgres;Port=5432;Database=houseos;Username=houseos;Password=${POSTGRES_PASSWORD:-houseos-dev}"
      Fichiers__Chemin: /app/donnees/fichiers
      # Clé du endpoint /mcp — obligatoire en prod, à définir dans le .env du serveur
      # (générer avec : openssl rand -hex 32)
      Mcp__Cle: ${HOUSEOS_MCP_KEY:?HOUSEOS_MCP_KEY requis (openssl rand -hex 32, dans le .env)}
    ports:
      - "8080:8080"
    volumes:
      - fichiers:/app/donnees/fichiers   # manuels et photos des équipements (inclus au backup)

volumes:
  postgres-data:
  fichiers:
