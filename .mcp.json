{
  "mcpServers": {
    "house-os": {
      "type": "http",
      "url": "${HOUSEOS_MCP_URL:-https://houseos.alainnormandin.dev/mcp}",
      "headers": {
        "Authorization": "Bearer ${HOUSEOS_MCP_KEY}"
      }
    }
  }
}
