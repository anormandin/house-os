{
  "mcpServers": {
    "house-os": {
      "type": "http",
      "url": "${HOUSEOS_MCP_URL:-http://localhost:5000/mcp}",
      "headers": {
        "Authorization": "Bearer ${HOUSEOS_MCP_KEY:-dev-cle-mcp-houseos}"
      }
    }
  }
}
