# Vault

This directory is the project's spec vault — its source of truth. Before editing
anything here, invoke the global `vault` skill and follow its contract.
Start from [[Home]] (Home.md); never bulk-read the vault.
